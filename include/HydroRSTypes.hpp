#ifndef HYDRORSTYPES_HPP_
#define HYDRORSTYPES_HPP_

enum class Commands {
    SetPumpState = 0,
    SetLampState = 1,
};

enum class Requests {
    RequestTankData = 0,
    RequestUpperData = 0
};

#endif